{
  "name": "unraid-cron-jobs",
  "type": "module",
  "scripts": {
    "build.assets": "NO_BUILD=true NO_PUSH=true bun run build.ts",
    "build.all": "NO_PUSH=true bun run build.ts",
    "build.all.local": "LOCAL=true NO_PUSH=true bun run build.ts",
    "publishProjects": "bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@qodestack/utils": "^1.4.0",
    "cheerio": "^1.0.0-rc.12",
    "cron": "^3.1.7"
  }
}
